请只对 `input/dirty-orders.xlsx` 做数据体检，不要修改、覆盖或另存清洗后的原表。请生成 `outputs/data-health-report.xlsx`，逐项记录问题类型、工作表/行/字段、原值、问题说明、建议动作、处理级别和是否需要人工确认；再生成 `outputs/fix-plan.md`，把建议分为“可自动修复”“建议修复”“必须人工确认”。

请检查重复、空值、单位与币种混用、日期格式、前后空格、字段名、明显异常值等问题。只把确定且不改变业务含义的操作列为可自动修复；业务含义不确定或看似异常但可能真实的记录要保留并说明为什么需要人工确认，不要猜测正确值。
