请扫描当前工作区 `input/` 里的所有文件，结合文件名和可读取内容判断文件类型与业务类别，先在 `outputs/organization-plan.md` 写清分类和统一命名规则，再把每个文件的副本整理到 `outputs/organized/` 下，并生成 `outputs/file-index.md` 或 `outputs/file-index.csv`。索引要能从原文件名追溯到新路径，并说明判断依据。

不要删除、移动、改名或覆盖 `input/` 中的原文件，也不要把多个版本擅自合并。无法可靠判断归属、日期或版本的文件请放入 `outputs/organized/NEEDS_REVIEW/`，保留原文件名并写明需要人工确认的问题；不要猜。

