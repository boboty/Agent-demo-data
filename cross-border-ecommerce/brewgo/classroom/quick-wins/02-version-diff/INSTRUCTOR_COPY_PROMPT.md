请比较 `input/supplier_quote_v3.xlsx` 和 `input/supplier_quote_final.xlsx`，按 SKU 与供应商对齐记录，只报告真正变化，并区分新增、删除和修改。请生成 `outputs/change-report.xlsx` 和 `outputs/change-summary.md`；明细中写出旧值、新值和变化字段，摘要中指出可能影响采购、成本、交付或现金安排的高风险变化。

不要修改或覆盖两份原始 Excel，也不要按行号直接比较。风险判断请说明依据并作为待业务人员复核的提示；字段含义、匹配关系或影响无法确定时要明确暴露，不要猜。

