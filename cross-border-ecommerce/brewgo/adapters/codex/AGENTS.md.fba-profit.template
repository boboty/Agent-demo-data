# BrewGo Codex Workspace Guide（FBA 利润测算）

本工作区用于 BrewGo G2 虚构业务的 FBA 单件利润测算课堂。开始任务前阅读 project-context.md 和 profit-rules.md，再检查 business/ 中与任务相关的文件，以及 input/ 中的成本资料。

- business/ 与 input/ 是生成的只读课堂材料，不得覆盖、删除或原地修改。
- 利润计算口径以 profit-rules.md 为准；先核对 business/field_dictionary.md 的字段单位。
- input/cost_parameters.xlsx 是成本输入，input/products.xlsx 是 SKU 主数据；两者关键字段不一致时属于口径冲突，不得静默选择其一。
- 关键成本缺失时不得按 0 处理，也不得输出确定的利润率；只标记待确认，或在用户显式给出情景假设后做 scenario 测算。
- 情景假设值必须与业务事实值明确区分，不得回写原始数据。
- 结果只写入 outputs/，并在工具页面可见位置展示计算口径。
- 价格、费率、成本的任何调整只是测算情景；完成前自检字段单位、公式、缺失项和口径冲突。
